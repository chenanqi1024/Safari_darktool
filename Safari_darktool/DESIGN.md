# DESIGN.md

## 定位

本文件是通用 Safari 插件 UI 规范。它不描述具体功能需求，也不要求 1:1 复刻某个项目；具体能力、字段和验收标准应放在 `SPEC.md`。

设计目标是让插件看起来像一个克制、可靠、原生的 macOS/Safari 工具。

## 基本原则

- 优先使用系统字体、系统控件和系统颜色。
- 界面应轻量、直接，不做营销式 landing page。
- 用户能一眼看出插件当前状态、主要操作和是否需要手动启用。
- 只展示当前场景需要的信息，不把实现细节暴露给用户。
- 状态变化、保存失败和权限不可用时，应有简短、可执行的反馈。
- 文案使用短句，避免解释性长段落。

## Host App

host app 主要承担安装后引导、扩展状态展示和 Safari 设置入口。

建议包含：

- 插件名称和图标。
- 当前扩展状态。
- 简短启用说明。
- 打开 Safari 设置的主按钮。
- 必要的隐私或本地处理提示。

host app 不应默认承担完整设置中心、账号中心或营销页面职责，除非产品范围明确需要。

## Toolbar Popup

popup 是用户日常控制入口，应适合快速操作。

设计建议：

- 把最常用操作放在最上方。
- 使用 segmented control、switch、checkbox、slider 等熟悉控件表达对应操作。
- 当前网站相关操作应显示清楚的站点上下文。
- 无法操作时禁用相关控件，并给出简短原因。
- 设置变化后尽量即时生效；不能即时生效时告知用户下一步。

## 页面内 UI

页面内浮层、按钮或提示应谨慎使用。

- 默认不遮挡网页主要内容。
- 视觉上保持轻量，避免强装饰、强阴影或大面积背景块。
- 支持关闭、隐藏或收起。
- 不加载远程 UI 资源，除非产品范围和隐私声明明确允许。

## 可访问性

- 交互控件可键盘访问。
- 状态不能只依赖颜色表达。
- 文本与背景保持可读对比度。
- icon-only 按钮需要可访问标签或 tooltip。
- 动画和过渡应克制，并尊重减少动态效果设置。
